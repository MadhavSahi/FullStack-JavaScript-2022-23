const header = 
//this is called react fragment..the empty tags..we need to have the HTML in under 1 tag only.
//the browser will render all the tags inside...the babel will render only the 1st tag..which is <>
<>
<p>Hi React.</p>
<p>Hello React.</p>
</>